version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10.11.0
    steps:
      - checkout
      - run: |
          yarn run lint
          yarn run build
          yarn run test-and-send-cov-to-coveralls
